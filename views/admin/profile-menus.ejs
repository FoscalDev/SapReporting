<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - SAP Reports</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f0f2f5;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
        }
        
        .breadcrumb {
            margin-bottom: 1rem;
            color: #666;
            font-size: 0.9rem;
        }
        
        .breadcrumb a {
            color: #0070f3;
            text-decoration: none;
        }
        
        .breadcrumb a:hover {
            text-decoration: underline;
        }
        
        .card {
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }
        
        .card-header {
            padding: 1.5rem;
            border-bottom: 1px solid #eeeeee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .card-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: #333;
            margin: 0;
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
            transition: all 0.3s ease;
            margin-right: 0.5rem;
            margin-bottom: 0.5rem;
        }
        
        .btn-primary {
            background-color: #0070f3;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #005bb5;
        }
        
        .btn-secondary {
            background-color: #6c757d;
            color: white;
        }
        
        .btn-secondary:hover {
            background-color: #5a6268;
        }
        
        .alert {
            padding: 0.75rem 1.25rem;
            margin-bottom: 1rem;
            border: 1px solid transparent;
            border-radius: 0.25rem;
            font-size: 0.9rem;
        }
        
        .alert-success {
            color: #155724;
            background-color: #d4edda;
            border-color: #c3e6cb;
        }
        
        .alert-error {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
        }
        
        .menu-structure {
            display: grid;
            gap: 1.5rem;
        }
        
        .menu-level {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 1rem;
        }
        
        .menu-level h3 {
            margin: 0 0 1rem 0;
            color: #333;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .menu-item {
            background: white;
            border: 1px solid #dee2e6;
            border-radius: 6px;
            padding: 1rem;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .menu-item-info {
            display: flex;
            align-items: center;
            gap: 1rem;
            flex: 1;
        }
        
        .menu-item-icon {
            font-size: 1.2rem;
            width: 2rem;
            text-align: center;
            color: #0070f3;
        }
        
        .menu-item-details h4 {
            margin: 0 0 0.25rem 0;
            font-size: 1rem;
            color: #333;
        }
        
        .menu-item-details p {
            margin: 0;
            font-size: 0.8rem;
            color: #666;
        }
        
        .checkbox-container {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .checkbox-container input[type="checkbox"] {
            width: 18px;
            height: 18px;
            cursor: pointer;
        }
        
        .level-indicator {
            display: inline-block;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.7rem;
            font-weight: 600;
            color: white;
            margin-left: 0.5rem;
        }
        
        .level-1 {
            background-color: #0070f3;
        }
        
        .level-2 {
            background-color: #28a745;
        }
        
        .level-3 {
            background-color: #ffc107;
            color: #212529;
        }
        
        .empty-state {
            text-align: center;
            padding: 2rem;
            color: #666;
        }
        
        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #ccc;
        }
        
        .form-actions {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 1.5rem;
            margin-top: 2rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="breadcrumb">
            <a href="/">Dashboard</a> > 
            <a href="/admin/users">Administración</a> > 
            <a href="/admin/profiles">Perfiles</a> > 
            Configurar Menús
        </div>

        <div class="card">
            <div class="card-header">
                <h1 class="card-title">
                    <i class="fas fa-bars"></i>
                    Configurar Menús del Perfil: <%= profile.name %>
                </h1>
                <a href="/admin/profiles" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i>
                    Volver a Perfiles
                </a>
            </div>
            <div class="card-body">
                <% if (success && success.length > 0) { %>
                    <div class="alert alert-success">
                        <%= success %>
                    </div>
                <% } %>
                
                <% if (error && error.length > 0) { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                <% } %>

                <form method="POST" action="/admin/profiles/<%= profile._id %>/menus/update" id="menuForm">
                    <!-- Estructura de menús por niveles -->
                    <div class="menu-structure">
                        <!-- Nivel 1 -->
                        <div class="menu-level">
                            <h3>
                                <i class="fas fa-layer-group"></i>
                                Nivel 1 - Menús Principales
                                <span class="level-indicator level-1">Padre</span>
                            </h3>
                            <% if (allMenus.filter(menu => menu.level === 1).length > 0) { %>
                                <% allMenus.filter(menu => menu.level === 1).forEach(function(menu) { %>
                                    <div class="menu-item">
                                        <div class="menu-item-info">
                                            <div class="menu-item-icon">
                                                <i class="<%= menu.icon %>"></i>
                                            </div>
                                            <div class="menu-item-details">
                                                <h4><%= menu.text %></h4>
                                                <p>Ruta: <%= menu.route %> | Orden: <%= menu.order %></p>
                                            </div>
                                        </div>
                                        <div class="checkbox-container">
                                            <input type="checkbox" 
                                                   name="menuItems" 
                                                   value="<%= menu._id %>" 
                                                   id="menu_<%= menu._id %>"
                                                   <%= assignedMenuIds.includes(menu._id.toString()) ? 'checked' : '' %>>
                                            <label for="menu_<%= menu._id %>">Asignar</label>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="empty-state">
                                    <i class="fas fa-folder-open"></i>
                                    <p>No hay elementos de nivel 1</p>
                                </div>
                            <% } %>
                        </div>

                        <!-- Nivel 2 -->
                        <div class="menu-level">
                            <h3>
                                <i class="fas fa-layer-group"></i>
                                Nivel 2 - Submenús
                                <span class="level-indicator level-2">Hijo</span>
                            </h3>
                            <% if (allMenus.filter(menu => menu.level === 2).length > 0) { %>
                                <% allMenus.filter(menu => menu.level === 2).forEach(function(menu) { %>
                                    <div class="menu-item">
                                        <div class="menu-item-info">
                                            <div class="menu-item-icon">
                                                <i class="<%= menu.icon %>"></i>
                                            </div>
                                            <div class="menu-item-details">
                                                <h4><%= menu.text %></h4>
                                                <p>Ruta: <%= menu.route %> | Padre: <%= menu.parentId %> | Orden: <%= menu.order %></p>
                                            </div>
                                        </div>
                                        <div class="checkbox-container">
                                            <input type="checkbox" 
                                                   name="menuItems" 
                                                   value="<%= menu._id %>" 
                                                   id="menu_<%= menu._id %>"
                                                   <%= assignedMenuIds.includes(menu._id.toString()) ? 'checked' : '' %>>
                                            <label for="menu_<%= menu._id %>">Asignar</label>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="empty-state">
                                    <i class="fas fa-folder-open"></i>
                                    <p>No hay elementos de nivel 2</p>
                                </div>
                            <% } %>
                        </div>

                        <!-- Nivel 3 -->
                        <div class="menu-level">
                            <h3>
                                <i class="fas fa-layer-group"></i>
                                Nivel 3 - Sub-submenús
                                <span class="level-indicator level-3">Nieto</span>
                            </h3>
                            <% if (allMenus.filter(menu => menu.level === 3).length > 0) { %>
                                <% allMenus.filter(menu => menu.level === 3).forEach(function(menu) { %>
                                    <div class="menu-item">
                                        <div class="menu-item-info">
                                            <div class="menu-item-icon">
                                                <i class="<%= menu.icon %>"></i>
                                            </div>
                                            <div class="menu-item-details">
                                                <h4><%= menu.text %></h4>
                                                <p>Ruta: <%= menu.route %> | Padre: <%= menu.parentId %> | Orden: <%= menu.order %></p>
                                            </div>
                                        </div>
                                        <div class="checkbox-container">
                                            <input type="checkbox" 
                                                   name="menuItems" 
                                                   value="<%= menu._id %>" 
                                                   id="menu_<%= menu._id %>"
                                                   <%= assignedMenuIds.includes(menu._id.toString()) ? 'checked' : '' %>>
                                            <label for="menu_<%= menu._id %>">Asignar</label>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="empty-state">
                                    <i class="fas fa-folder-open"></i>
                                    <p>No hay elementos de nivel 3</p>
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i>
                            Guardar Configuración de Menús
                        </button>
                        <a href="/admin/profiles" class="btn btn-secondary">
                            <i class="fas fa-times"></i>
                            Cancelar
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        // Funcionalidad para seleccionar/deseleccionar todos los elementos de un nivel
        document.addEventListener('DOMContentLoaded', function() {
            const levels = document.querySelectorAll('.menu-level');
            
            levels.forEach(level => {
                const header = level.querySelector('h3');
                const checkboxes = level.querySelectorAll('input[type="checkbox"]');
                
                // Agregar funcionalidad de seleccionar todos
                header.addEventListener('click', function() {
                    const allChecked = Array.from(checkboxes).every(cb => cb.checked);
                    
                    checkboxes.forEach(checkbox => {
                        checkbox.checked = !allChecked;
                    });
                });
                
                // Cambiar cursor del header
                header.style.cursor = 'pointer';
            });
        });
    </script>
</body>
</html>
